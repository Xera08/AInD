# AInD workspace

- Full-stack TypeScript project: React/Vite client, Express server, Prisma ORM.
- Use npm scripts from the repository root.
- Keep game rules and campaign state server-authoritative as the AI DM engine grows.
- Prefer focused, typed modules over large route or component files.
